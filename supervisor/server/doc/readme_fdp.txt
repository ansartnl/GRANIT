ƒл€ установки SupervisorServer сервиса нужно использовать install.bat
ƒл€ деинстал€ции сервиса SupervisorServer - uninstall.bat
«апуск сервиса можно выполнить с использованием start.bat, restart.bat или с помощью
консоли "Servce Control Manager"
ќстановка сервиса выполн€етс€ или с использованием stop.bat или с помощью "Servce Control Manager"

ќписанией файла конфигурации:

1. ‘айл конфигурации содержит 3 секции и 2 параметра.
   ƒл€ рабочего места CWP и FDP файлы конфигурации отличаютс€.
   —екции:
   - fmtp_services:
     указываютс€ FMTP сервисы и пор€док их запуска: им€ тега - FMTP сервис, значение - пор€док запуска
   - fdp_services:
     указываютс€ FDP сервисы и пор€док их запуска: им€ тега - FDP сервис, значение - пор€док запуска
   - database - параметры соединени€ с базой данных, запуск которой будет валидироватьс€
   - language - english или russian
   - log - состо€ние логировани€.  огда значение true - выполн€ть логирование, иначе - не выполн€ть
2. ‘айл конфигурации может находитс€:
   - в текущей папке с сервисом и должен имет им€ сервиса SupervisorServer.xml
   - в папке ../etc/config.xml
   - передаватьс€ первым аргументом в командной строке

ќрганизаци€ работы сервиса:
- ¬ыполн€етс€ попытка соединени€ с базой данных до тех пор пока не будет установлено соединение
- «апускаютс€ FMTP сервисы в пор€дке указанном в конфигурации
- «апускаютс€ FDP сервисы в пор€дке указанном в конфигурации
≈сли в процессе работы возникает критическа€ ситуаци€, например FMTP сервисы перестают работать
SupervisorServer в течении 5 сек выполн€ет перезагрузку сервисов и таким образом восстанавливает
их работоспособность.
“о же самое относитс€ и к FDP сервисам (интервал проверки работоспособности сервиса - также 5 сек).
